# 参与贡献

感谢你对 **ThinkingSpace** 感兴趣！这份文档说明如何参与，以及需要遵守的规则。

---

## 一、先做这三件事

1. **先开 Issue 讨论。** 较大的改动（新功能、重构、改数据模型）请先开一个 Issue 说明想法，
   确认方向后再动手，避免白做。
2. **签署 CLA。** 本项目采用[贡献者许可协议](./CLA.md)。
   在 Pull Request 描述里写明「我已阅读并签署 CLA」即可。
3. **遵守[行为准则](./CODE_OF_CONDUCT.md)。**

> 只改错别字、修文档这类小改动，可以直接提 PR，不必先开 Issue。

---

## 二、本地开发

```bash
npm install
npm run dev        # http://localhost:5173
```

**改完必须三项全绿**，否则不会被合并：

```bash
npm run typecheck
npm run build
npm run smoke
```

`npm run smoke` 是无浏览器冒烟测试，覆盖了所有纯逻辑（树遍历、上下文构建、
差异算法、思考回放、跨项目搬运、用量估算等）。**新增纯逻辑时请顺手补测试。**

---

## 三、必须遵守的规则

- **不要破坏 `AGENTS.md` 里的架构铁律**（同步只在 `src/lib/cloud/` 一层、
  项目内容整体存 `content jsonb`、迁移唯一入口等）。
- **绝对不要提交任何密钥**：API Key、数据库密码、`.env.local` 都不行。
  密钥只保存在浏览器 localStorage，永远不进仓库。
- **不要引入不必要的依赖**。这个项目刻意保持依赖精简。
- **不要大面积重构无关文件**。一个 PR 只做一件事。
- **遵守设计原则**：AI 提议用户决定、内容优先、低视觉噪声、克制。
  详见 README 的「设计原则」。

---

## 四、代码风格

- TypeScript 严格模式，`noUnusedLocals` / `noUnusedParameters` 已开启。
- 注释与界面文案使用中文，和现有风格保持一致。
- 注释只写「为什么」，不写「做了什么」——后者应该由代码本身表达。
- 组件放在 `src/components/`，纯逻辑放在 `src/lib/`（便于测试）。

---

## 五、提交 Pull Request

请使用仓库的 PR 模板，其中包含一份检查清单。

- 一个 PR 只解决一个问题
- 描述里说明「改了什么、为什么改、怎么验证」
- 关联对应的 Issue（`Closes #12`）

---

## 六、你的贡献如何授权

你的贡献会以 [AGPL-3.0](./LICENSE) 发布给所有人，
同时按 [CLA](./CLA.md) 授予项目作者重新授权（含商业授权）的权利。

你**保留**自己贡献的著作权，你的名字会被保留在 Git 历史中。
