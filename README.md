# ThinkingSpace

> 把 AI 对话从一条时间线，变成一个可以探索的思维空间。

**在线体验**：https://tommy-hang.github.io/Thinkingspace/ · 当前版本 **V0.4.3**

<sub>
缘起：这个项目来自一次早八课前真实的思考火花——「为什么我明明想深入一个问题，最后却总是在翻聊天记录？」
原型由完全的 Vibecoding 在一天内搭出，之后持续迭代维护。故事不是重点，下面才是。
</sub>

---

## 一、它要解决的问题

今天绝大多数 AI 产品，仍然沿用即时通讯软件的结构：一条不断向下滚动的消息流。

这种形式极其简单，也因此非常成功。但它与「复杂思考」之间有一个越来越明显的矛盾：

> **AI 的能力已经从「回答一个问题」进化为「辅助完成复杂研究」，而承载它的交互方式仍然是线性聊天记录。**

具体来说，有三个结构性缺陷：

### 缺陷 1：局部问题污染主线

```text
你正在读：Positional Encoding 的作用
        ↓ 突然对回答里的 QKV 产生疑问
        ↓ 追问 5 轮
        ↓
   原本的主线已经滚出屏幕，找不回来了
```

一个小的疑问，打断了你正在进行的理解。

### 缺陷 2：关系被压扁成顺序

```text
思考的真实结构              聊天记录里的样子

概念 A                      A
├─ 问题 A1                  A1
└─ 问题 A2                  A2
概念 B                      B
└─ 问题 B1                  B1
```

从属关系消失了，只剩时间顺序。

### 缺陷 3：上下文越滚越脏

传统做法把**全部历史**发给模型。轮次越多：

- Token 成本线性增长
- 无关内容稀释模型注意力
- 模型越来越难判断「你这句话在问什么」

---

## 二、我们的答案：改变最基本的单位

> **不再是「消息」，而是「主题」。**

```text
                    Transformer
                         │
        ┌────────────────┼────────────────┐
        │                │                │
   Attention      Positional Enc.       FFN
        │
   ┌────┴────┐
   │         │
  QKV    Multi-head
```

这张图不是事后自动生成的「漂亮思维导图」——**它就是你和 AI 进行思考的主要工作空间。**

### Topic · 主题卡片

一个认知主题 = **一张卡片**，而不是一条消息。

```text
┌────────────────────────────┐
│ Multi-head Attention       │
│ 为什么需要多个 Head？       │
│                            │
│ 6 轮 · 3 个分支 · 待解决 1  │
└────────────────────────────┘
```

卡片内部仍然可以有很多轮 Q&A，但它们**被关在这张卡片里**，不会污染别的主题。

> 200 条消息 ≠ 200 个节点。否则地图会迅速失去意义。

### Branch · 分支

在 AI 回答里**选中任意一句话**，就能从它生长出一个新的子主题。

而且分支会记住它**来自哪句话**：

```text
source node   →  Attention 机制
source message →  第 4 轮回答
anchor text    →  "The attention score is scaled by √dₖ"
```

所以模型知道你在问什么，而不需要猜「这里」指哪里。

### Map · 地图

所有主题构成一张**可以导航的地图**。

你不仅记得「我讨论过这个」，还记得**「它在思考空间的哪个位置」**——
这就是人类记忆里被无限滚动彻底放弃的**空间记忆**。

---

## 三、最核心的创新：结构本身成为 AI 的上下文

这是 ThinkingSpace 与所有「AI 聊天 + 可视化」产品的根本区别。

### 传统做法

```text
全部历史  →  全部发送给模型
```

### ThinkingSpace 的做法

```text
项目概述
   ↓  沿树向上，只取当前主题的祖先
上层主题摘要（每层截断）
   ↓
当前主题内部的对话
   ↓
分支锚点（你从哪句话分出来的）
   ↓
你现在的问题
```

**换句话说：用户看到的主题树，本身就是模型的上下文检索结构。**

```text
信息架构  =  用户的认知结构  =  模型的上下文结构
```

这三者第一次高度一致。带来的直接好处：

| | 效果 |
| --- | --- |
| Token 成本 | 只发相关内容，不随历史线性增长 |
| 回答质量 | 没有无关分支的噪声干扰 |
| 精确性 | 锚点让模型知道你具体在问哪一句 |

---

## 四、不只是发散，还能收敛

大多数 AI 工具只擅长「继续往下」。但真正的思考需要两步：

```text
发散  →  收敛
```

ThinkingSpace 两者都支持：

| 能力 | 说明 |
| --- | --- |
| **当前理解** | 每个主题由 AI 总结出的**结论性**概述（不是机械摘要） |
| **综合理解** | 把一个主题下的多个子分支，**收敛**成更高层的理解 |
| **知识地图** | 由全部卡片的「当前理解」生成整个项目的**知识点思维导图**，支持导出 PDF |
| **待解决问题** | 不只记录「我知道了什么」，也记录「我还不知道什么」——全项目共享一份清单 |

```text
回答 → AI 提议方向 → 你选择深入 → 形成理解 → 发现未解决问题
  ↑                                                    │
  └────────────── 综合理解（收敛）←────────────────────┘
```

---

## 五、AI 提议，用户决定

ThinkingSpace 不会变成「AI 自动创建几十个节点、自动连线、自动重构地图」的系统。

```
AI proposes  →  user decides
```

| AI 会做的 | AI 绝不会做的 |
| --- | --- |
| 提议 2–4 个值得继续探索的方向 | 自动创建节点 |
| 生成主题的「当前理解」 | 自动连接关系 |
| 综合子分支形成更高层理解 | 自动重构你的地图 |
| 建议一个知识点标题 | 覆盖你手动写下的标题 |

**地图始终属于用户。**

---

## 六、数据属于你

| 设计 | 说明 |
| --- | --- |
| **BYOK** | 每个用户使用自己的 AI API Key，站点不承担推理成本 |
| **不登录也能用** | 游客模式下内容只保存在本机浏览器 |
| **云端可选** | 登录后跨设备同步，数据库层强制隔离（RLS），只有本人能读 |
| **随时完整导出** | 一键导出全部项目为 JSON，不被平台锁定 |
| **开源** | 全部代码公开，可自行审查 |

---

## 功能一览

### 空间化的思考

Conversation Map · Topic Card · Focus View（带空间过渡）· Branch（含锚点）·
Branch Intent（深入解释 / 为什么 / 举例 / **反例** / 建立联系 / 自定义）·
子树折叠 · 隐藏与恢复 · 思考路径高亮 · 悬停预览 · 自动布局

### 收敛与整理

当前理解 · 综合理解 · 待解决问题 · AI 探索方向建议 · 知识地图（+ PDF 导出）· 分支 Markdown 导出

### 导航

最近浏览 · 收藏 · 全局搜索（`Ctrl+K`）· 主题直链 · 主题结构树

### AI 能力

多模型切换（DeepSeek-V4.1-Flash / V4-Pro 等，支持自定义模型名）·
深度思考开关与强度 · 流式输出 · 联网搜索（Tavily / Exa / Serper）·
Markdown + KaTeX 渲染 · 多 Provider（DeepSeek / OpenAI / OpenRouter / 任意 OpenAI 兼容）·
离线演示模式（不配置任何 Key 也能体验）

### 日常使用

项目管理 · 节点状态与筛选 · 撤销重做 · 编辑提问并重新生成 ·
导入导出 · 使用说明面板 · 隐私说明 · 深浅色主题 · GitHub / 邮箱登录

---

## 快速开始

```bash
npm install
```

```bash
npm run dev
```

然后打开 http://localhost:5173 —— 首次访问会有新手指引。

---

## 接入 AI

1. 点击右上角齿轮 → 设置
2. 在 **AI Providers** 中找到 DeepSeek，填入你的 API Key
3. 点「设为当前」
4. 回到主题中提问

**联网搜索**（可选）：设置 →「联网搜索」填入 Tavily / Exa / Serper 的 Key。

> 未配置 Key 时默认使用「离线演示」模式，界面流程完全可用。

---

## 数据与隐私

- 不登录时，内容只保存在**你自己浏览器的 localStorage**
- 登录后，项目同步到云端（Supabase），**数据库层强制隔离，只有你能读到自己的数据**
- **API Key 只保存在本机浏览器**，不参与导出，也不进入代码仓库
- 导出的 `.thinkingspace.json` 只包含项目内容
- ⚠️ 清理浏览器数据会导致本机内容不可见，**请定期导出备份**

详见应用内 **设置 → 隐私与数据**。

---

## 常用命令

| 目的 | 命令 |
| --- | --- |
| 安装依赖 | `npm install` |
| 启动开发服务器 | `npm run dev` |
| 类型检查 | `npm run typecheck` |
| 生产构建 | `npm run build` |
| 冒烟测试（无需浏览器） | `npm run smoke` |
| 云端联调（需测试账号） | `npm run cloud-check` |
| 预览构建产物 | `npm run preview` |

---

## 技术栈

React 19 · TypeScript · Vite · Tailwind CSS v4 · @xyflow/react · zustand · Supabase

---

## 项目结构

```text
src/
  types.ts                  数据模型
  store/store.ts            全局状态与所有业务动作
  lib/
    ai/                     Provider 层 + Context Engine
    cloud/                  Supabase 连接 / 认证 / 同步（业务代码无感知）
    search/                 联网搜索
    reasoning.ts            当前理解 / 探索方向 / 综合理解
    knowledgeMap.ts         知识地图生成
    knowledgeExport.ts      知识地图导出 SVG / PDF
    title.ts                知识点标题与摘要
    tree.ts                 树遍历（祖先 / 后代 / 可见性）
    layout.ts               自动布局
    exportImport.ts         导出 / 导入
    storage.ts              本地持久化与数据迁移（唯一迁移入口）
  components/               界面组件
supabase/schema.sql         云端建表 + RLS 策略（可重复运行）
scripts/smoke.mjs           冒烟测试
scripts/cloud-check.mjs     云端同步联调
```

---

## 版本路线

- **V0.1 交互原型** ✅ Map / Topic Card / Focus View / Branch / 本地保存
- **V0.2 AI 原型** ✅ Provider 层 / 流式输出 / Context Engine / Branch Context
- **V0.3 个人日常使用** ✅ 项目管理 / 搜索 / 节点状态 / 布局 / 导入导出 / 设置
  - **V0.3.1** ✅ Markdown / KaTeX、V4 模型与思考模式、联网搜索
  - **V0.3.2** ✅ GitHub Pages 自动部署
  - **V0.3.3** ✅ 撤销重做 / 折叠 / 路径高亮 / 最近 / 收藏 / 直链 / 分支意图 / 导出 / 编辑重生成 / 悬停预览
  - **V0.3.4** ✅ 使用说明 / AI 自动生成知识点标题 / 右键菜单 / 隐藏机制
  - **V0.3.5** ✅ 当前理解 / 待解决问题 / 探索方向建议 / 综合理解 / @ 引用主题
  - **V0.3.6** ✅ 知识地图（知识点思维导图 + PDF 导出）
- **V0.4 云端** 🚧 Supabase Auth（GitHub / 邮箱）+ 跨设备云同步 + RLS 行级隔离
  - **V0.4.1** ✅ 按需加载（省 90% 流量）+ 定时保活
  - **V0.4.2** ✅ GitHub 账号一键登录
  - **V0.4.3** ✅ 隐私说明 · 新手指引
- **V0.5 公开测试** ⏳ 限流 / 监控 / 用量提示

---

## 设计原则

1. **AI 提议，用户决定。** 不自动创建节点、不自动连线、不自动重构地图。
2. **内容优先，低视觉噪声。** 地图是为思考服务的，不是为炫技服务的。
3. **上下文要精准，而不是越多越好。** 只发送与当前主题真正相关的内容。
4. **数据属于你。** 本地优先、密钥不上传、随时可完整导出。

---

## 部署

推送到 `main` 分支会自动构建并发布到 GitHub Pages：

```bash
git add -A
git commit -m "说明这次改了什么"
git push
```

进度见 [Actions](https://github.com/Tommy-hang/Thinkingspace/actions)，约 2–3 分钟后网站自动更新。
