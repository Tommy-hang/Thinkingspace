# 复制这个文件为 .env.local 并填入你自己的值（.env.local 不会进入 Git）
#
# Supabase 项目地址与公开密钥：
# Supabase 后台 → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 注意：anon key 是「公开密钥」，写在网页里是设计如此，安全性由数据库的
# Row Level Security 策略保证。千万不要把 service_role key 填在这里。
