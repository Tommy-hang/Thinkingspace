# 安全政策

## 一、报告安全问题

如果你发现安全问题（例如数据泄露、权限绕过、XSS、密钥泄露等），
**请不要直接开公开 Issue**——在修复之前公开细节会让所有用户暴露在风险中。

请改用下面任一方式：

1. **GitHub 私密报告（推荐）**
   https://github.com/Tommy-hang/Thinkingspace/security/advisories/new

2. **或**开一个不含细节的 Issue，只写「发现安全问题，请联系我」，
   作者会主动联系你。

作者会尽快回复，并在修复完成后公开说明（如果你愿意，会注明发现者）。

---

## 二、本项目涉及的安全边界

了解这些有助于你判断什么是「漏洞」：

| 设计 | 说明 |
| --- | --- |
| **API Key** | 只保存在用户自己的浏览器 localStorage，**从不上传、不参与导出** |
| **公开密钥** | 前端使用的 Supabase anon / publishable key 是**设计上公开**的，安全性由数据库 RLS 保证 |
| **数据隔离** | 云端数据由 PostgreSQL 行级安全（RLS）在**数据库层**强制隔离 |
| **传输** | 全部使用 HTTPS |
| **密码** | 由 Supabase Auth 处理，本项目不接触明文密码 |
| **无自建后端** | 没有服务器可被入侵；攻击面主要是浏览器与 Supabase 配置 |

**因此，「拿到了 anon key」不算漏洞**；但如果你能**读到别人的数据**、
**绕过 RLS** 或**越权修改**，那就是严重问题，请立即报告。

---

## 三、请不要在 Issue / PR 中粘贴

- ❌ API Key（`sk-…`、`tvly-…` 等）
- ❌ 数据库连接串 / 数据库密码
- ❌ 真实用户的邮箱、对话内容或其他个人信息
- ❌ `.env.local` 的内容

如果不小心粘贴了，请**立即到对应平台吊销该密钥**，然后再来联系作者。

---

## 四、支持的版本

只有 **README 顶部标注的最新版本**会得到安全修复。
报告问题前，请先确认你用的是最新版本（界面右上角显示版本号）。

---

## 五、致谢

感谢每一位负责任地报告问题的朋友。经你同意后，你的名字会出现在修复说明里。
